export interface IUser {
    address: { name: string, _id: string, city: string , state:string},
    createdAt: string;
    createdBy: string;
    primaryNumber:number,
    id: string;
    name: string;
    updatedAt: string;
    updatedBy: string;
    _id: string;
}